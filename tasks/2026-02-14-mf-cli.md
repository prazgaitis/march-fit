# MF CLI

**Date:** 2026-02-14
**Description:** Add a new `mf` CLI package under `packages/` that talks to the HTTP API, persists config (API base URL + default challenge), and allows challenge override via command params.

## TODO
- [x] Define CLI command shape (`mf` + subcommands/options)
- [x] Create workspace package for CLI in `packages/cli`
- [x] Implement config read/write and defaults
- [x] Implement HTTP client and auth handling
- [x] Implement challenge-aware commands with per-command override
- [x] Add usage docs and workspace scripts
- [x] Verify with lint/typecheck/basic run
