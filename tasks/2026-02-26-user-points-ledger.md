# 2026-02-26 User Points Ledger

Hidden page at `/challenges/[id]/users/[userId]/ledger` showing a full breakdown of all points for a user in a challenge.

## Requirements
- [x] Backend query returning all activities grouped by day with streak bonuses
- [x] Server component page (same auth pattern as profile)
- [x] Client component showing day-by-day breakdown
- [x] Not linked from anywhere (hidden page)
